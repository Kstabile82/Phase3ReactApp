//comes from ProjectCard component's ProjAnimals / ProjVolunteers
//accesses Volunteer & Animal components
//has a set state Assign
//if Assign, Volunteer & Animal list components have "+" button (if not already in the projectAnimal array)
    //if already in the array, have "-" button
//
//edit volunteers, edit animals buttons in project card that trigger component
//+ posts new project_animal or project_volunteer with the right IDs
//- deletes based on IDs